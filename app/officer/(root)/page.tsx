import React, { Fragment } from "react";

import clsx from "clsx";

const OfficerIndexPage = () => {
  return (
    <Fragment>
      <section>
        <div className={clsx([`container main-space-x`])}>Officer home</div>
      </section>
    </Fragment>
  );
};

export default OfficerIndexPage;
